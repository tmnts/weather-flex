# 🌦️ Погодный-Флекс: Фуллстэк MVP на монорепозитории

Погодная станция в реальном времени, построенная на микросервисе **Go** и реактивном фронтенде **Svelte 5** (он обновился). Высокая производительность, модульность и эстетический минимализм. Ваш UX/UI дизайнер будет в восторге.

## 🏗️ Обзор архитектуры

- **Управление монорепозиторием:** Используются **Turborepo** и **pnpm Workspaces** для бесшовной разработки на локальной машине и плотной общей конфигурации.
- **Бэкенд (Go 1.22):** Легковесный API, готовый к использованию в бессерверной архитектуре, который перенаправляет запросы в **OpenWeatherMap**, обрабатывая аутентификацию с помощью защищенных переменных среды.
- **Фронтенд (Svelte 5 + Tailwind v4):** Современный реактивный пользовательский интерфейс, использующий **руны** Svelte (`$state`, `$effect`) и собственную систему дизайна "Glassmorphism".
- **Облачная инфраструктура:** CI/CD на **Vercel** с автоматическим развертыванием бессерверных функций Go.

## 🛠️ Техно стэк


| Уровень | Технология | Почему? |
| :--- | :--- | :--- |
| **Фронтенд** | SvelteKit 5 | Реактивность нового поколения и превосходная DX-чувствительность. |
| **Бэкенд** | Go (Golang) | Выдерживает высокую нагрузку, типобезопасен, исполняемый файл минимального размера. |
| **Стили** | Tailwind CSS v4 | Быстрая разработка пользовательского интерфейса с использованием CSS, ориентированного на функциональность. |
| **Развертывание (деплой)** | Vercel | Поддержка глобальной периферийной сети и нативной среды выполнения Go. |
| **Система сборки** | Turborepo | Оптимизация пайплайнов и других задач. |

## 🚀 Ключевые сложности технического характера

1.  **CORS & Networking:** В процессе разработки был реализован мост **Vite Proxy** для решения проблем междоменного доступа между сервером разработки Svelte (:5173) и API Go (:3000).
2.  **Infrastructure as Code:** Настроены правила перезаписи в файле `vercel.json` для сопоставления бессерверных функций Go с единой конечной точкой (эндпойнтом) `/api`, что обеспечивает чистую структуру URL-адресов.
3.  **Resilience:** Устранены региональные ограничения API и проблемы с сетью (IPv6 против IPv4) путем стандартизации локальных подключений к IPv1.0.0.1.

## 📖 Локальная разработка

1.  Клонируйте репозиторий.
2.  Добавьте `OPENWEATHER_API_KEY` в вашу среду.
3.  Запустите `pnpm install`.
4.  Выполните команду `pnpm dev`, чтобы запустить конвейер (пайплайн) Turborepo.

## 📖 API документация (Swagger/OpenAPI)

Бэкенд документирует сам себя через **Swagger UI**, получается интерактивная песочница для пользователей фронтенда.
- **Интерактивный UI:** [https://weather-flex.vercel.app](https://weather-flex.vercel.app)
- **OpenAPI Спецификация:** Сгенерировано из комментариев исходного кода Go с использованием `swag init`.

### 🔄 Поток данных (Последовательность)
1. **Клиент** (Svelte 5) запрашивает `/api/weather`.
2. **Vercel** направляет запрос к **Go Serverless Function**.
3. **Go Middleware** проверяет путь `swagger` ; если тот отсутствует, вызывает Weather Handler (Обработчик погоды).
4. **Бэкенд** получает аутентифицированные данные из **OpenWeatherMap** (Server-to-Server).
5. **JSON Ответ** перенаправляется обратно клиенту, минуя региональные CORS ограничения.


---
*Сделано с чувством ❤️ к АйТи сообществу 2026.*